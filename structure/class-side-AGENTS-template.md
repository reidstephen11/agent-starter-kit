# Class-side AGENTS.md template

Save as `AGENTS.md` at the root of the class folder — the one holding student work. It is a
separate file from the planning-side `AGENTS.md` on purpose: different rules, different
risk. Copy from the line below.

---

# <Class code> — <Unit name> student working folder

This folder holds identifiable student work in school-managed storage.

## Privacy boundary (non-negotiable, read before anything else)

- Everything in the student folders is identifiable student work.
- **Never copy** student names, folder IDs, document content or evidence out of this
  folder, into any other workspace, or into any external service. No uploads, no cloud
  agent mode, no personal accounts, no pasting into a chat window.
- **Folder IDs only** in reports, profiles, filenames and prompts. Never a full name, and
  never a name-to-ID mapping anywhere in this folder.
- If a class list or enrolment file appears here, do not read, quote or analyse it.
- Wellbeing, disclosure, bullying, self-harm or child-safety material: do not quote it, do
  not respond to it, do not write it into any shared report. Tell me privately and stop.
- `_TEST` is a fake student and `_ARCHIVE` holds withdrawn students — exclude both from
  every sweep, statistic and report.

## What you may and may not do here

1. **Read-only by default.** Writing into a student's document happens only when I have
   explicitly turned it on for that job.
2. **Never edit student-authored content** — not spelling, not formatting, not tidying.
3. **Never assign** a grade, rating or status; never tick a milestone; never sign anything
   off. Recommend, with evidence, and I decide.
4. **Skip and report** any locked (`~$`), missing, misnamed or unreadable file. Never
   create a missing student's document.
5. Say what you could not check. Silence reads as "fine".

## The weekly pass

The procedure is `<path>/workflows/feedback-pass.md`. Follow it step by step, in order,
every time. Do not improvise a shorter version.

## Local facts the pass depends on

- **Student document template:** `<name>`, using named fields — <describe the field naming
  convention, e.g. tagged content controls named `did_NN`, `reflect_NN`, `fb_note_NN`>.
  Read and write **by field name**, never by searching for placeholder text.
- **Feedback fields (agent-writable, when writing mode is on):** <list them>
- **Student-authored fields (never touched):** <list them>
- **`WEEKS.md`** maps calendar dates to unit weeks and milestone targets.
- **Feedback house style:** <two glows, one next, word limits, ban list — or point at the
  planning-side style file>.
